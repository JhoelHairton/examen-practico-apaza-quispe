# Dependencias Python — Laboratorios 1 y 3
# Instalar con:  pip install -r requirements.txt
# (versiones verificadas en el entorno de desarrollo; >= permite parches)

pandas>=2.2.1
numpy>=1.26.4
matplotlib>=3.10.0
seaborn>=0.13.2
scikit-learn>=1.4.0
joblib>=1.3.0

# Lab 3 (Jupyter Notebook)
jupyter>=1.0.0
notebook>=7.0.0
nbconvert>=7.0.0
ipykernel>=6.0.0
